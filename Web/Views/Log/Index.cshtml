@model AllLogsViewModel
@{
    ViewBag.Title = "Logs";
}

<h2>Logs</h2>

<ul>
    @foreach (var logFile in Model.Logs)
    {
        <li>@Html.RouteLink(logFile.LogDateDisplay + "(" + logFile.NumLines + ")", "ViewDateLog", new { date = logFile.LogDateName })</li>
    }
</ul>