<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - DIY RSS</title>

    <link rel="stylesheet" href="@Fingerprint.Tag("/Content/bootstrap.min.css")" />
    <link rel="stylesheet" href="@Fingerprint.Tag("/Content/site.css")" />
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet" />

    @RenderSection("Styles", required: false)
    
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">

        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="@Url.RouteUrl("Home")">
                    <img src="@Fingerprint.Tag("/Content/diyrss.png")" alt="DIY RSS">
                </a>
                @Html.RouteLink("DIY RSS", routeName: "Home", routeValues: null, htmlAttributes: new { @class = "navbar-brand" })
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.RouteLink("My Feeds", "MyFeeds")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - DIY RSS</p>
        </footer>
    </div>
    <script src="@Fingerprint.Tag("/Scripts/jquery-1.10.2.min.js")"></script>
    <script src="@Fingerprint.Tag("/Scripts/bootstrap.min.js")"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
